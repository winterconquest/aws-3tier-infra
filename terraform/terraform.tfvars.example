# 이 파일을 terraform.tfvars로 복사한 뒤 값을 채워 사용한다.
# terraform.tfvars는 .gitignore 대상이므로 커밋되지 않는다.
#
# cp terraform.tfvars.example terraform.tfvars

# RDS 마스터 자격증명
db_username = "admin"
db_password = ""    # 8자 이상

# CloudWatch 알람 수신 주소
# apply 후 SNS 구독 확인 메일의 링크를 클릭해야 알람이 발송된다
alert_email = ""